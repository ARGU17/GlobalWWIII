export const proposeEconomy=country=>{const products=Object.values(country.v5?.economy?.products||{}),shortage=products.sort((a,b)=>(b.shortage||0)-(a.shortage||0))[0];if(shortage?.shortage>20)return{type:"secure_supply",productId:shortage.id,urgency:shortage.shortage};if(country.economy.debtRatio>110)return{type:"debt_reduction",urgency:country.economy.debtRatio-100};return{type:"investment",sector:"productivity",urgency:30}};
